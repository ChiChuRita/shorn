---
// Empty component: replaces both ThemeSelect and ThemeProvider to force dark-only
// mode. The picker is the control; the provider is the inline script that would
// otherwise set data-theme from prefers-color-scheme. Removing only one of the two
// leaves light-mode visitors on Starlight's light ink ramp over the dark ground.
---
